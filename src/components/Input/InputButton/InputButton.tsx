import css from '../../../styles/InputButton.module.css';

const InputButton: React.FunctionComponent = () => {
	return <button className={css.input_button}>Generate</button>;
};

export default InputButton;
